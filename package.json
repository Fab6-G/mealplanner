{
  "name": "mealplanner",
  "version": "1.0.0",
  "description": "",
  "main": "js/data.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "private": true,
  "devDependencies": {
    "wrangler": "^4.95.0"
  }
}
