{
  "name": "mealplanner",
  "version": "1.0.0",
  "description": "Cloudflare Workers REST API for Couples Meal Planner backed by D1",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply DB --local",
    "migrate:remote": "wrangler d1 migrations apply DB --remote"
  },
  "devDependencies": {
    "wrangler": "^4.95.0"
  },
  "private": true
}
